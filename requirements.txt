# CryoNav — Antarctic Sea-Ice, Iceberg Trajectory & Navigation Decision Support
# Python 3.11+ | Apple Silicon (MPS) / CUDA / CPU

# --- Core scientific stack ---
numpy>=1.24,<2.0
scipy>=1.11
xarray>=2023.6
zarr>=2.16
dask[complete]>=2023.6
netCDF4>=1.6
h5netcdf>=1.2

# --- Geospatial ---
pyproj>=3.6
cartopy>=0.22
rioxarray>=0.15
shapely>=2.0

# --- ML ---
torch>=2.1
scikit-learn>=1.3

# --- Visualisation ---
matplotlib>=3.7
cmocean>=3.0

# --- API ---
fastapi>=0.104
uvicorn[standard]>=0.24
httpx>=0.25

# --- Utilities ---
pyyaml>=6.0
tqdm>=4.66
requests>=2.31
numba>=0.58
pandas>=2.0

# --- Optional data access (fail gracefully if missing) ---
# cdsapi>=0.7.7
# copernicusmarine>=1.0
